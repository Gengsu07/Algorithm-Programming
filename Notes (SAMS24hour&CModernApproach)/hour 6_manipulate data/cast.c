/* 06L04 Using the cast operator */ 
#include <stdio.h> 
int main() 
{      
    int x, y;
    x=7;
    y=5;

    printf("Given x = %d, y = %d\n", x, y);
    printf("x / y produc %d\n", x / y); 
    printf("(float)x / y produc %f\n", (float)x / y); return 0;
return 0;
}